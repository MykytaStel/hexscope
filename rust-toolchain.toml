[toolchain]
# Pinned, not "stable": a floating channel let a new clippy lint on CI's
# newer compiler fail the build with no code change. Bump deliberately.
channel = "1.98.0"
components = ["rustfmt", "clippy"]
targets = ["wasm32-unknown-unknown"]
